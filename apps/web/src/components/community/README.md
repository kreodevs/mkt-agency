# Community Manager (UI)

- `CommunityManagerPrerequisites.tsx` — checklist de perfil de empresa para mejorar precisión del copy IA.

Preferencias de plataformas y cantidad de posts se persisten en `tenant.settings.communityManager` vía `PUT /community-manager/preferences`.
