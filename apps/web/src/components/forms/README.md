# Componentes — formularios

| Componente | Uso |
|------------|-----|
| `FormSnippet.tsx` | Muestra y copia snippet JS/HTML de embed |
| `FormListCard.tsx` | Tarjeta mobile para listado en `/forms` (snippet + eliminar) |

API: `GET /forms/:id/snippet`.
