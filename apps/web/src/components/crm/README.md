# Componentes CRM

| Archivo | Rol |
|---------|-----|
| `LeadPipeline.tsx` | Kanban por etapa con score |
| `LeadDetail.tsx` | Detalle con historial; layout `card` (sidebar desktop) o `plain` (sheet) |
| `LeadDetailMobileSheet.tsx` | Bottom-sheet en `< lg` al seleccionar tarjeta |
| `CreateLeadDialog.tsx` | Alta manual de lead (email obligatorio) |

Ruta: `/leads`. Visible en menú SOHO y Growth.
