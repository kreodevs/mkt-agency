# Ajustes del copiloto

`/settings/copilot` — preferencias de redes, volumen semanal y **generación automática** (cron lunes 06:00 por tenant, desactivada por defecto). Delega en `PUT /community-manager/preferences` (`autoWeeklyGenerationEnabled`). El botón «Preparar mi día/semana» en Inicio sigue funcionando con el cron desactivado.
